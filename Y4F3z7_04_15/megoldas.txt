CREATE OR REPLACE TYPE szemely_t AS OBJECT (
    nev VARCHAR2(100),

    MEMBER FUNCTION bemutatkozas RETURN VARCHAR2,
    STATIC FUNCTION min_kor RETURN NUMBER,
    ORDER MEMBER FUNCTION osszehasonlit (p_masik szemely_t) RETURN INTEGER
) NOT FINAL;
/


CREATE OR REPLACE TYPE BODY szemely_t AS

    MEMBER FUNCTION bemutatkozas RETURN VARCHAR2 IS
    BEGIN
        RETURN 'A nevem: ' || SELF.nev;
    END;

    STATIC FUNCTION min_kor RETURN NUMBER IS
    BEGIN
        RETURN 18;
    END;

    ORDER MEMBER FUNCTION osszehasonlit (p_masik szemely_t) RETURN INTEGER IS
    BEGIN
        IF SELF.nev < p_masik.nev THEN
            RETURN -1;
        ELSIF SELF.nev > p_masik.nev THEN
            RETURN 1;
        ELSE
            RETURN 0;
        END IF;
    END;

END;
/




2///////////////////////////////////////////////////////
CREATE OR REPLACE TYPE foszakacs_t UNDER szemely_t (
    fkod VARCHAR2(50),
    eletkor NUMBER,

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2
);
/

CREATE OR REPLACE TYPE BODY foszakacs_t AS

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2 IS
    BEGIN
        RETURN 'Főszakács vagyok. A nevem: ' || SELF.nev ||
               ', korom: ' || SELF.eletkor;
    END;

END;
/

CREATE OR REPLACE TYPE vendeg_t UNDER szemely_t (
    vkod VARCHAR2(50),
    eletkor NUMBER,

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2
);
/

CREATE OR REPLACE TYPE BODY vendeg_t AS

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2 IS
    BEGIN
        RETURN 'Vendég vagyok. A nevem: ' || SELF.nev;
    END;

END;
/

3/////////////////////////////////////////

CREATE TABLE etterem_szemelyek_tbl OF szemely_t;
/


INSERT INTO etterem_szemelyek_tbl
SELECT vendeg_t(
           x.nev,
           x.vkod,
           x.eletkor
       )
FROM XMLTABLE(
    '/etterem/vendegek/vendeg'
    PASSING XMLTYPE(:xml_input)
    COLUMNS
        nev      VARCHAR2(100) PATH 'nev',
        vkod     VARCHAR2(50)  PATH 'vkod',
        eletkor  NUMBER        PATH 'eletkor'
) x;



INSERT INTO etterem_szemelyek_tbl
SELECT foszakacs_t(
           x.nev,
           x.fkod,
           x.eletkor
       )
FROM XMLTABLE(
    '/etterem/foszakacsok/foszakacs'
    PASSING XMLTYPE(:xml_input)
    COLUMNS
        nev      VARCHAR2(100) PATH 'nev',
        fkod     VARCHAR2(50)  PATH 'fkod',
        eletkor  NUMBER        PATH 'eletkor'
) x;


SELECT VALUE(t) FROM etterem_szemelyek_tbl t;

SELECT t.bemutatkozas()
FROM etterem_szemelyek_tbl t;



4///////////////////
SELECT szemely_t.min_kor() AS minimum_eletkor
FROM dual;

SELECT t.nev,
       t.bemutatkozas() AS bemutatkozas
FROM etterem_szemelyek_tbl t
ORDER BY VALUE(t);

SELECT t.nev
FROM etterem_szemelyek_tbl t
WHERE VALUE(t) IS OF (ONLY vendeg_t);


SELECT t.nev,
       TREAT(VALUE(t) AS vendeg_t).eletkor AS eletkor
FROM etterem_szemelyek_tbl t
WHERE VALUE(t) IS OF (vendeg_t);



=======================================================================================================

Házi feladat



CREATE OR REPLACE TYPE szakacs2_t UNDER szemely_t (
    szkod   VARCHAR2(50),
    reszleg VARCHAR2(100),

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2
);
/

CREATE OR REPLACE TYPE BODY szakacs2_t AS

    OVERRIDING MEMBER FUNCTION bemutatkozas RETURN VARCHAR2 IS
    BEGIN
        RETURN 'Szakács vagyok a(z) ' || SELF.reszleg ||
               ' részlegen, a nevem: ' || SELF.nev;
    END;

END;
/

2///////////////////////////////////////////////////
INSERT INTO etterem_szemelyek_tbl
SELECT szakacs2_t(
           x.nev,
           x.szkod,
           x.reszleg
       )
FROM XMLTABLE(
    '/etterem/szakacsok/szakacs'
    PASSING XMLTYPE(:xml_input)
    COLUMNS
        nev      VARCHAR2(100) PATH 'nev',
        szkod    VARCHAR2(50)  PATH 'szkod',
        reszleg  VARCHAR2(100) PATH 'reszleg'
) x;

SELECT t.nev,
       t.bemutatkozas()
FROM etterem_szemelyek_tbl t
WHERE VALUE(t) IS OF (szakacs2_t);


3/////////////////////////////////

SELECT t.bemutatkozas() AS bemutatkozas
FROM etterem_szemelyek_tbl t
WHERE VALUE(t) IS OF (foszakacs_t, szakacs2_t);

WHERE VALUE(t) IS OF (ONLY foszakacs_t, ONLY szakacs2_t);


